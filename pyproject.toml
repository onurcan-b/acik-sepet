[project]
name = "acik-sepet"
version = "0.1.0"
description = "Experimental open daily grocery price index for Turkey"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
